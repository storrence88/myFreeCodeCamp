{"index.js":"var convertToRoman = function(num) {\n\n  // Create arrays with default conversion with matching indices.\n  var decimalValue = [ 1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1 ];\n  var romanNumeral = [ 'M', 'CM', 'D', 'CD', 'C', 'XC', 'L', 'XL', 'X', 'IX', 'V', 'IV', 'I' ];\n\n  //empty string variable for the final roman number\n\n  var romanized = '';\n\n  // Loop through the indices of the decimalValue array.\n  for (var index = 0; index < decimalValue.length; index++) {\n    console.log(`Index is ${index}`);\n    console.log(\"\");\n    // Continue to loop while the value at the current index will fit into numCopy\n    while (decimalValue[index] <= num) {\n      console.log(`decimalValue[index] is ${decimalValue[index]}`);\n      console.log(`Romanized before adding is ${romanized}`);\n      // Add the Roman numeral & decrease numCopy by the decimal equivalent.\n      romanized += romanNumeral[index];\n      console.log(`Romanized after adding is ${romanized}`);\n      console.log(\"\");\n      console.log(`Num before subtracting is ${num}`);\n      num -= decimalValue[index];\n      console.log(`Num after subtracting is ${num}`);\n    }\n  }\n\n  return romanized;\n}\n\nconvertToRoman(36);"}